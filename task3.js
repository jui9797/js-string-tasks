/**
 * Task-3:
Check whether a string contains all the vowels a, e, i, o, u
 */
let baby ='a little baby is sleeping in a bed.'

    
    if(baby.includes('a' && 'e' && 'i' && 'o' && 'u')){
        console.log('the string contain all the vowels')
    }
    else{
        console.log('the string doesnot contain all the vowels')
    }
